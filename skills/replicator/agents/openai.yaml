interface:
  display_name: "Three.js Replicator"
  short_description: "Recreate Three.js effects from multimodal references with evidence gates, structured routing, and fidelity review"
  default_prompt: "Use $replicator (Three.js Replicator) to analyze multimodal references such as URLs, keywords, image URIs, screenshots, or local images; enforce the reference access gate before any faithful remake, classify the mode contract explicitly, build a visual evidence table, use classic graphics sources only as a completion-depth baseline, escalate only material route tradeoffs with AskUserOption, choose the implementation surface, post pipeline, and performance contract, and finish with localhost browser validation plus reference-vs-current capture review in REPORT.md."
