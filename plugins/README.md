# Plugins

This directory is reserved for heavier plugin packages that should not live inside `skills/`.

Current state:

- No standalone plugin package is published from this directory yet.
- Repository host metadata currently lives under `.claude-plugin/` and `.cursor-plugin/`.
